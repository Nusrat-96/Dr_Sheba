from django.urls import path, include

# This file only contains URL patterns that are NOT at root
# The landing page is at root (defined in drsheba/urls.py)
# Allauth URLs are also at /accounts/ (defined in drsheba/urls.py)
